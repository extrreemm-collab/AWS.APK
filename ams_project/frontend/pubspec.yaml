name: ams_frontend
description: "AMS lecturer mobile client."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  google_sign_in: ^7.2.0
  google_sign_in_web: ^1.1.3
  google_sign_in_platform_interface: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
